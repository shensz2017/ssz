# Python Dependencies

Install the required libraries with:

```bash
pip install PyQt6 requests opencv-python numpy
```

## Notes
- `PyQt6` provides the desktop UI.
- `requests` is used for calling the API.
- `opencv-python` and `numpy` handle local image processing.
